import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.idxgaza.wamda',
  appName: 'وَمضة',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
